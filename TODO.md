# k2o-wg-p2p-connector — TODO

> **Last updated:** 2026-07-02 (scaffold rollout — K2O Standardization v2)
> Done items live in `log.md`. This file = active + pending only.
> Public repo — see `~/.claude/k2o/public-repos.md` leak-guard before touching.

## Active

- [ ] `CLAUDE.md`'s "## What" section still holds the placeholder text
  `<one-line purpose — refine during migration>` — never filled in after the
  K2O-standard wiring commit (`98d9f31`). Needs a real one-liner.

## Backlog

No other open items found. The script is feature-complete per `README.md` /
`ARCHITECTURE.md` (auto role detection, key exchange, port-conflict handling,
firewall integration, 3x-confirm removal, manual fallback) and has no
outstanding roadmap or known-bug notes in the repo. Treat as stable/maintenance
mode until a new feature request or bug report surfaces.
